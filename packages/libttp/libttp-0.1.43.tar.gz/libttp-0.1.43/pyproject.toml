[build-system]
requires = ["setuptools",
            "wheel",
            "cython>=0.29.30",
            "numpy>=1.22.0",
            "tqdm",
            "pandas",
            "scipy"]
build-backend = "setuptools.build_meta"
