[project]
name = "guji"
version = "0.1.0"
description = "guji guji why?"
authors = [
    {name = "Nicou11", email = "lyoungin1204@gmail.com"},
]
dependencies = [
    "pytz>=2024.2",
]
requires-python = ">=3.7"
readme = "README.md"
license = {text = "MIT"}

[project.scripts]
guji = "guji.guji:now"

[tool.pdm]
distribution = true
