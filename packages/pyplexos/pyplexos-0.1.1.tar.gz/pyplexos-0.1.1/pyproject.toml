[project]
name = "pyplexos"
version = "0.1.1"
description = "PLEXOS interface"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ibis-framework[duckdb]>=9.5.0",
    "pandas>=2.2.2",
    "panel>=1.5.0",
    "pythonnet>=3.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
]
