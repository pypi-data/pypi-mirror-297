# setup.py
from setuptools import setup

setup(
    name="kukufuckingkan",
    version="0.2",
    packages=["audio"],
    install_requires=[
        # List your dependencies here
        "requests",
        "flask",
    ],
)
