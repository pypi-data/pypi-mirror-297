[tool.poetry]
name = "vagr"
version = "0.0.1"
description = "Serverless platform for LLMs"
authors = ["vagrant.ai"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
