[tool.poetry]
name = "marshal-poetry-demo"
version = "0.1.1"
description = ""
authors = ["Marshal Wu <marshal.wu@gmail.com>"]
readme = "README.md"
license = "MIT"

[tool.poetry.scripts]
my_demo = 'marshal_poetry_demo.main:run'

[tool.poetry.dependencies]
python = "^3.10"
streamlit = "^1.38.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
