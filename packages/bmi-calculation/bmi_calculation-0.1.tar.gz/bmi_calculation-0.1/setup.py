from setuptools import setup

setup(name="bmi_calculation",
      version='0.1',
      description='Calculation Body Mass Index',
      author='sfgrahman',
      packages=['bmi_cal'],
      zip_safe=False)