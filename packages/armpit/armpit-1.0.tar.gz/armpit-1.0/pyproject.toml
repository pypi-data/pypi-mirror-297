[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "armpit"
version = "1.0"
description = "Python incremental revision"
readme = "README.md"

[project.scripts]
armpit = "armpit:main"
