[tool.poetry]
name = "libtokit"
version = "0.1.3"
description = "A simple utility for creating directories."
authors = ["pytools <hyhlinux@gmail.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
